# Быстрый старт — CloudShop CRUD тесты

## 🚀 Запуск тестов

### Все тесты (7 штук):
```bash
cd playwright_project
source venv/bin/activate
pytest tests/ -v
```

### Отдельные тесты:

**1. Создание товара:**
```bash
pytest tests/products/test_create_product.py -v -s
```

**2. Редактирование товара:**
```bash
pytest tests/products/test_update_product.py -v -s
```

**3. Удаление товара:**
```bash
pytest tests/products/test_delete_product.py -v -s
```

## 📋 Описание тестов

### 1️⃣ test_create_product.py
Создание товара с заполнением **всех доступных полей**:
- Наименование, штрих-код, артикул
- Цены (закупка, наценка, продажа)
- Габариты (высота, ширина, глубина, вес)
- Минимальный остаток
- Searchable dropdown: единица измерения, страна, код налога (опционально)

### 2️⃣ test_update_product.py
Редактирование товара:
1. Создает тестовый товар
2. Кликает по строке товара → открывается модальное окно
3. Нажимает "Редактировать"
4. Изменяет название и цену
5. Сохраняет
6. Проверяет обновление

### 3️⃣ test_delete_product.py
Удаление товара:
1. Создает тестовый товар
2. Выбирает чекбокс товара
3. Открывает dropdown "Действия"
4. Нажимает "Удалить"
5. Подтверждает удаление (кнопка "Да")
6. Проверяет перемещение в корзину

## ⚠️ Важно

- **Удаление**: В CloudShop товары не удаляются полностью, а перемещаются в корзину (`/card/trash/`)
- **Авторизация**: Убедитесь, что в файле `.env` указаны корректные данные:
  ```env
  CLOUDSHOP_EMAIL=your_email@example.com
  CLOUDSHOP_PASSWORD=your_password
  ```

## 📊 Ожидаемый результат

```
tests/auth/test_login.py::test_login_success PASSED
tests/auth/test_login.py::test_login_invalid_password PASSED
tests/auth/test_login.py::test_login_empty_fields PASSED
tests/auth/test_login.py::test_logout PASSED
tests/products/test_create_product.py::test_create_product_with_all_fields PASSED
tests/products/test_update_product.py::test_edit_product PASSED
tests/products/test_delete_product.py::test_delete_product PASSED

======================== 7 passed ========================
```

## 🎯 Маркеры pytest

Все тесты товаров помечены как:
- `@pytest.mark.products` — тесты модуля товаров
- `@pytest.mark.smoke` — критические smoke тесты
- `@pytest.mark.P0` — приоритет 0 (блокирующие)

Запуск только smoke тестов:
```bash
pytest -m smoke -v
```

